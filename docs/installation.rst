.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install voice_bot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv voice_bot
    $ pip install voice_bot
